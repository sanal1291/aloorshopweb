class Category {
  String name;
  Map displayNames;
  dynamic image;
  List<dynamic> searchArray;
  int priority;
  Category({this.name, this.image, this.searchArray, this.priority, this.displayNames});
}
