class Item {
  String name, picture, category, uid;
  num price;
  List<dynamic> searchArray;
  Item(
      {this.name,
      this.picture,
      this.category,
      this.searchArray,
      this.uid,
      this.price});
}
