class CartItem {
  num quantity, total;
  String quality, itemId, name, picture;
  CartItem(
      {this.itemId,
      this.quality,
      this.quantity,
      this.total,
      this.name,
      this.picture});
}
